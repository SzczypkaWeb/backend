2026-07-27 — PATCH instead of PUT for user updates: partial updates are more convenient for the client; idempotency is guaranteed by merge-not-delta semantics, not by the choice of HTTP method.
2026-07-27 — GET /users pagination/search: query params are validated and transformed via FindUsersQueryDto (page/limit coerced to numbers, defaults 1/10, limit capped at 100); `search` does a case-insensitive partial match on email. Enabled `transform: true` on the global ValidationPipe so query strings are converted to the DTO's typed fields before reaching the controller. Response is wrapped in `{ data, total, page, limit, totalPages }` rather than a bare array, so clients can render pagination controls without a separate count request.
2026-08-06 — Staging deploy as a separate Cloud Run service (`backend-staging`), separate workflow file (`deploy-gcp-staging.yml`, triggered on push to `staging`) rather than a branch-conditional job inside `deploy-gcp.yml`: zero risk of a staging-only change accidentally affecting the production deploy path. Staging secrets are separate GCP Secret Manager entries (`*_STAGING` suffix) pointing at a dedicated Supabase project, not the production database - needs manual setup in GCP/Supabase before this workflow can succeed (not yet done).
